package com.alchemist.config;

import org.springframework.context.annotation.Configuration;


/*@EnableSwagger2*/
@Configuration
public class SwaggerConfig {
	/*
	 * @Bean //To customizd docket object @Bean used public Docket apiDoc() { return
	 * new Docket(DocumentationType.SWAGGER_2).select()
	 * .apis(RequestHandlerSelectors.basePackage("com.alchemist.rest"))
	 * .paths(PathSelectors.any()) //in class generate documentation for all
	 * methods, we can mention post or get .build(); }
	 */

}
